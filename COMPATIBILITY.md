# CuteMamen 兼容性规范

本文档定义 **CuteMamen 插件标准** 的兼容性契约。任何声称 "CuteMamen 兼容" 的系统，
必须实现本文所述的生命周期钩子、`manifest.json` 格式与插件包格式。

> 本文与 [README.md](./README.md) 中的 "CuteMamen 插件标准" 章节配套阅读。

---

## 1. 核心架构

CuteMamen 是一种轻量级、模块化的 AI 系统架构：

- **N 个独立训练的专家插件** —— 每个插件是一个自包含的、面向特定任务的模块
- **一个轻量级固定内核（Nest）** —— 仅负责路由、生命周期管理和内存调度
- **可热加载的 `.CuteMamen` 插件包** —— 插件可在运行时加载、卸载和替换，无需重启宿主系统

只有被激活的插件消耗算力，空闲的专家零成本。

## 2. 兼容性总原则

> **可以往插座上多加孔，但不能把已有的孔堵上。**

- 新增字段和钩子永远欢迎，向后兼容，无需升级版本号。
- 删除或修改已有契约属于**破坏性变更**，必须升级主版本号，并在解码器层做适配。

### 2.1 解码器层集中兼容

所有向前/向后兼容逻辑集中在单一层 —— **解码器**，其他模块无需感知插件版本：

```text
v1 插件 → 解码器（翻译为内部统一格式）→ 内核 → 各模块
v2 插件 → 解码器（直接透传，零开销）  → 内核 → 各模块
v3 插件 → 解码器（直接透传，零开销）  → 内核 → 各模块
```

由此获得三个性质：

1. **内核和各模块**永远只看到一种内部格式，对版本无感知。
2. **旧插件**只要解码器保留适配逻辑就能继续运行。
3. **新插件**不经过任何翻译层，性能无损；移除旧版支持只需删除解码器中的一个函数。

### 2.2 标准演进规则

当标准发生演进时（如 v1 → v2）：

1. **允许破坏性变更** —— 新标准可以丢弃遗留字段、简化接口
2. **解码器吸收迁移成本** —— 通过 `manifest.json` 检测插件版本并自动适配
3. **提供迁移工具** —— `migrate-v1-to-v2` 自动转换旧版插件包（见 README）
4. **公布废弃时间窗口** —— 旧插件触发警告，附带明确的停止支持日期

## 3. 插件包格式（`.CuteMamen`）

插件包是单个归档文件（tar.gz），内含：

```text
my-plugin.CuteMamen
├── manifest.json      # 必需：清单与版本声明
├── weights/           # 必需：插件权重
└── memory/            # 可选：三级记忆存档（工作 / 情景 / 语义）
```

DistributedFormer 的 `.dfpkg` 模态面存档（v0.7.0+）遵循本包格式的精神：
单归档 + `manifest.json` + `weights/` + `memory/`。

## 4. `manifest.json` 格式

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `standard_version` | string | 是 | 插件遵循的标准版本（语义化版本） |
| `name` | string | 是 | 插件唯一标识 |
| `base_model` | string | 否 | 基础模型类型（v2；v1 中为 `model_type`） |
| `capability` | string | 否 | 人类可读的能力描述 |
| `author` | string | 否 | 作者 |
| `memory_budget` | number | 否 | 内存预算上限（字节），超出触发淘汰 |
| `min_core_version` | string | 否 | 所需最低内核版本，内核过旧应拒绝加载 |

规则：

- 未知字段一律**保留不丢弃**（前向兼容）。
- v1 的 `model_type` 由解码器自动映射为 `base_model`；v1 的 `legacy_mode` 已废弃，迁移时自动删除。

## 5. 生命周期钩子

兼容系统必须实现并按以下顺序调用：

| 钩子 | 调用时机 | 必需 | 说明 |
|------|---------|------|------|
| `on_load` | 插件加载完成 | 是 | 初始化资源。v1 中为 `on_init`，迁移时自动重命名 |
| `on_think` | 事件到达、插件被路由激活 | 是 | 核心计算，接收统一内部格式的输入 |
| `on_unload` | 插件卸载 | 是 | 释放资源并持久化记忆。v2 起必需；v1 迁移时自动注入空实现 |

## 6. 与 Transformer 系统的集成路径

CuteMamen 设计了多种与现有架构兼容的集成方式：

- **LoRA 适配器映射** —— 每个 `.CuteMamen` 插件映射为一个 LoRA 适配器，Transformer 基础模型充当内核
- **外部服务桥接** —— 插件作为独立进程运行，通过标准 API（OpenAI 兼容或 gRPC）调用
- **MoE 专家注册** —— 插件注册为可动态加载的混合专家（MoE）单元，在推理时按需调度

## 7. 兼容性检查清单

一个系统要声称 "CuteMamen 兼容"，必须满足：

- [ ] 支持加载 / 卸载 `.CuteMamen` 插件包（tar.gz，含 `manifest.json`）
- [ ] 实现并正确调用 `on_load` / `on_think` / `on_unload` 生命周期钩子
- [ ] 遵守 `manifest.json` 格式规范，忽略未知字段
- [ ] 在解码器层集中处理版本适配，内核与模块对插件版本无感知
- [ ] 尊重 `min_core_version` 与 `memory_budget` 声明

---

*本文档随标准演进更新。破坏性变更遵循第 2.2 节的演进规则。*
